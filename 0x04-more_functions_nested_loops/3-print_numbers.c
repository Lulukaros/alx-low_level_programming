#include "main.h"
/**
*print_numbers - prints out the numbers from 0 to 9
*Return: 0 (success)
*/
void print_numbers(void)
{
	int n;

	for (n = 0; n <= 9; i++)
	{
		_putchar(n);
	}
	_putchar('\n');

	return (0);
}
