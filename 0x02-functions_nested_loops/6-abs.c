#include "main.h"
/**
*_abs - computes the absolute value of an integer
*@e: takes the value of the integer
*Return: Always 0
*/
int _abs(int e)
{
if (e < 0)
{
return (-e);
}
else
{
return (e);
}
return (0);
}
